FROM node as builder

RUN mkdir /usr/src/app
WORKDIR /usr/src/app
ENV PATH /usr/src/app/node_modules/.bin:$PATH

COPY ./console/package.json /usr/src/app/package.json
RUN npm install --silent

COPY ./console/ /usr/src/app/
RUN sed 's/{Your_API_URL}/http:\/\/localhost:8090/g' /usr/src/app/.env.prod.tmp > /usr/src/app/.env.prod
RUN npm run build


FROM nginx:latest

RUN mkdir /app
WORKDIR /app
RUN mkdir ./build
COPY --from=builder /usr/src/app/build ./build
RUN rm /etc/nginx/conf.d/default.conf
COPY ./Dockerfile/console/nginx.conf /etc/nginx/conf.d

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]